apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
    tier: frontend
    env: production
spec:
  containers:  # spec can have multiple images
    - name: nginx
      image: nginx
#    - name: busybox
#      image: busybox